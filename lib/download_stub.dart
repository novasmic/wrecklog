void downloadTextFile(String filename, String content) {
  // Not supported on Android/desktop in this build path.
  // (We’ll later replace this with Share/Save if you want.)
  throw UnsupportedError('Download is only supported on Web for now.');
}
